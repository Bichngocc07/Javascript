//Xây dựng hàm nhận vào 2 tham số là giá trị 
//của 2 số nguyên bất kì. Hàm thực hiện tính 
//tổng và trả về giá trị.
//Khai bao
function numbers(a,b){
    if(numbers.isInteger(a)&& numbers.isInteger){
        let total=a+b;
    }
    console.log(total);
}